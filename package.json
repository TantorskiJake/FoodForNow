{
  "name": "foodfornow",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "npm run --prefix=server dev",
    "client": "npm run --prefix=client start"
  },
  "license": "ISC",
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "es-errors": "^1.3.0"
  }
}
